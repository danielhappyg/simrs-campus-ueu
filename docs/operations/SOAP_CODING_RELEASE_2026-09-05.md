# SOAP, coding, registration, and admission release

## Scope

Daniel requested the remaining annotated outpatient medical-form changes and deployment of the combined update on 5 September 2026. This release includes the pending dashboard, registration, SATUSEHAT administrative-gender, and physician-signed admission changes from the same working session. It does not activate warehouse, BPJS, SATUSEHAT submission, or other live clinical integrations.

The medical form uses explicit Subjective, Objective, Assessment, and Plan labels. Assessment includes diagnosis free text, one primary ICD-10 selection, and secondary selections. Plan includes ICD-9-CM procedure selections. Selection is explicit; the application does not infer diagnoses or procedures from narrative text. Codes and descriptions persist in draft and immutable final document versions and remain visible after reload and in history.

Terminology lookup uses the [WHO ICD-10 2010 browser](https://icd.who.int/browse10/2010/en) and [NLM Clinical Tables ICD-9-CM procedures](https://clinicaltables.nlm.nih.gov/apidoc/icd9cm_sg/v3/doc.html). WHO ICD-10 is not substituted with ICD-10-CM. Searches send only the entered lookup query, not the patient's record or SOAP narrative, to the fixed provider. This hosted application remains synthetic-only. Search results require human selection; the server validates code/display pairs against provider responses and caches verified pairs. External-provider outages produce an unavailable response rather than fabricated codes. This is not an offline terminology catalog or certified coding engine.

Older documents lacking coded fields remain readable and compatible. New structured submissions validate diagnosis text, primary and secondary selections, duplicate codes, and procedure entries. Final records cannot be rewritten through ordinary draft saves.

## Local evidence

- Browser rehearsal in a separate disposable SQLite database: physician login, WHO `R42` lookup and explicit selection, ICD-9-CM `89.01` lookup and selection, SOAP draft save, and reload preserving both selected codes succeeded.
- Initial broad outpatient/inpatient/emergency/registration/database run: 371 passed, two skipped, one historical migration-rehearsal error. The test replayed an older SQLite table rebuild with newer graph triggers installed; its setup now removes and reinstalls only those newer guards. The corrected migration tests passed (2 tests / 30 assertions). No production guard was weakened.
- Finance/pharmacy/laboratory/radiology/operations regression run: 181 passed / 2,787 assertions. Two stale finance expectations were reconciled with the existing explicit system-administrator bypass; ordinary administrator and mixed-role denials remain tested.
- Outpatient plus corrected migration rerun: 111 passed, one skipped / 1,307 assertions.
- Structured-coding positive/negative HTTP tests: 7 passed / 30 assertions, including forbidden roles, malformed and unknown codes, primary-secondary duplication, legacy compatibility, and final immutability.
- Final isolated PostgreSQL 17 run of coding, disposition, admission, gender, and immutable-evidence tests: 21 passed / 127 assertions, including negative validation cases.
- Full frontend suite: 205 passed across 33 files. Production asset build passed. The final rebuilt form was inspected in the browser: DOM/keyboard order follows SOAP with diagnosis coding after Assessment and procedures after Plan; redundant disabled coding tabs were removed. The restrained layout preserves the existing hospital interface.

## Release requirements and current boundary

The hosted production-demo alias was verified at `a4fc7372966d99859ed9c4c8d1e083d79f19e9de`, deployment `dpl_fegZhwJsNQn4e7b6gUC4bB5jqwXU`, before this release. Supabase project `xbmsfvstcpngizcplqyg` uses private schema `laravel`. Read-only preflight found 41 patients, all marked synthetic, with only the two recognized legacy gender codes. The sex column is an unconstrained varchar, compatible with the migration.

Apply only the three migrations listed in [the admission implementation note](OUTPATIENT_ADMISSION_AND_GENDER_2026-09-05.md). Their PostgreSQL SQL was generated by executing the actual migrations on a disposable local database. The two deferred warehouse migrations must remain pending. Vercel requires the newly built committed `public/build` assets.

The safe sequence is: freeze and test the release commit; prepare its deployment; activate and verify the shared maintenance drain; wait for prior writers to finish; back up affected retained synthetic records and validate recovery; apply the scoped migrations; verify schema, ledger, and exact deployment SHA; reopen and run public and authenticated smoke checks.

**Release preparation resumed with Daniel's explicit backup approval.** A private backup of the 41 synthetic patient identifiers and original gender values was captured, with the migration ledger. An isolated conversion-and-recovery check restored every original value. This is a scoped gender-column recovery backup, not a full database snapshot; other existing columns are not changed by the release migrations. No record contents or backup file are included in Git. Hosted migration and production deployment remain pending until the release sequence below completes.

Rollback of the application alone is insufficient after conversion to canonical gender codes. Keep maintenance active if migration or promotion is ambiguous; inspect the ledger and prefer a forward correction. Do not run generic migration rollback, reset the retained database, or reseed existing actors. Record the final commit, deployment ID, migration batch, smoke results, and recovery evidence here after release actually succeeds.
