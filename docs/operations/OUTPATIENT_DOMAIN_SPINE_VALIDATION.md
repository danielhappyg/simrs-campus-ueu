# Outpatient Domain-Spine Validation Record

- **Date:** 2026-07-16
- **Increment:** complete synthetic outpatient reference journey through finalized debrief, shared teaching-note evidence, and finalized report working references
- **Decision records:** ADR-003, ADR-004, ADR-005, and ADR-006
- **Release status:** pull-request candidate; not a clinical or faculty-pilot release

## Automated evidence

| Check                                                                              | Result                                                                                |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------- |
| PHP feature/unit suite                                                             | Passed: 147 tests, 1,825 assertions                                                   |
| PHPStan                                                                            | Passed: zero errors                                                                   |
| Pint                                                                               | Passed                                                                                |
| TypeScript                                                                         | Passed                                                                                |
| ESLint                                                                             | Passed                                                                                |
| Prettier                                                                           | Passed                                                                                |
| React unit/accessibility suite                                                     | Passed: 15 files, 30 tests                                                            |
| Production Vite build                                                              | Passed                                                                                |
| Composer manifest validation                                                       | Passed                                                                                |
| Composer advisory audit                                                            | Passed: no security vulnerability advisories found                                    |
| npm advisory audit                                                                 | Passed: zero vulnerabilities                                                          |
| SQLite fresh migration and opt-in demo seed                                        | Passed                                                                                |
| Local MySQL 9.7 migration, full journey, backup/restore, and restored health check | Passed; see [Local MySQL and Recovery Validation](LOCAL_MYSQL_RECOVERY_VALIDATION.md) |
| MySQL 8.4 fresh migration and backend suite                                        | Passed in draft PR #10                                                                |

The backend suite includes direct endpoint denial, model-invariant, transaction, idempotency, state-machine, public-projection, finalized-release, data-minimization, chronological-attribution, exact correction-source routing, append-only debrief-note lineage, finalized-report source projection and capability/context gates, and synthetic-data tests. The frontend suite includes axe checks for the permanent simulation banner, patient-context banner, sign-in validation state, coding workspace, and finalized debrief presentation. It also guards single-main landmark ownership, one named native checkbox control, native checkbox form semantics, the shared narrow/coarse-pointer target-size rule, mobile-sidebar rendering and focus return, the complete sign-in sequence `passkey → email → password → visibility → reset → remember → submit`, programmatic sign-in error associations, the finalized debrief work-task contract and safe icon fallback, shared-note participant visibility and facilitator attestation, explicit non-scoring rubric status, report navigation, one authentication-page `h1`, wrapping of long machine-readable encounter reasons, required/error-linked correction-reason fields in nursing, medical, and closure authoring, and the registration workspace's one-case-per-session disabled state while the existing case remains available for check-in.

## Structured code-review findings resolved

Before publishing the increment, the implementation was reviewed for security, correctness, performance, and maintainability. The following issues were corrected:

- registration form state now adopts the next server-issued request key after success;
- new versus existing identity verification uses server-enforced semantics;
- inactive sessions and revoked assignments are rechecked inside locked domain transactions;
- direct encounter-status mutation is rejected;
- appointment, assignment, encounter, queue, transition, and work-task case boundaries are guarded;
- registration is serialized and the database enforces one encounter per isolated scenario run;
- the encounter rail marks only states present in the recorded transition history;
- public queue and registration-list queries are bounded;
- public queue data is unavailable after a simulation session closes;
- clinical review, encounter closure, and record-quality workspaces no longer create nested `main` landmarks inside the application shell;
- the shared checkbox now exposes one named native form control instead of an additional unnamed accessibility-tree checkbox; and
- narrow or coarse-pointer controls receive a 44 CSS-pixel minimum block target, with icon buttons also receiving a 44 CSS-pixel minimum inline target;
- the finalized `DEBRIEF` and procedure-correction task types are represented in the frontend contract and encounter orbit, with a safe icon fallback for future server task types;
- the mobile navigation sheet keeps its accessible title inside the dialog, exposes a visible close control, uses Indonesian action names, and returns focus to its trigger after Escape;
- the phone-width authentication view exposes a visible page-level heading and a natural keyboard order that includes the localized password-visibility control;
- the sign-in page no longer autofocuses email past the passkey option, and both validation messages are programmatically associated with their fields;
- long machine-readable encounter-transition reasons wrap instead of clipping the mobile timeline;
- every clinical correction form now exposes its attributed change reason as a required, error-linked field; and
- blocked coder tasks retain the exact diagnosis or performed-procedure source public ID, link to that source, and make the active correction source the coding workspace default instead of falling back to the first diagnosis;
- successful shared-note creation clears the authoring fields and adopts the next server-issued request key, allowing a different note to be submitted without a page reload;
- successful debrief-note revision closes the editor, clears its reason and attestation, and adopts the next server-issued revision key;
- the latest append-only version shows its own change reason immediately instead of hiding it until a later version exists; and
- the collapsed debrief history summary provides a 44 CSS-pixel mobile target.

No unresolved critical or high code-review finding is known in this bounded increment.

## Browser and manual evidence

The original in-app browser attempt rejected navigation to the local QA origin `http://127.0.0.1:8765`; no alternate workaround was used. Later supported in-app sessions on 16 July 2026 successfully opened isolated local applications at `http://127.0.0.1:8000` and `http://127.0.0.1:8020` and authenticated with synthetic training accounts.

The earlier responsive baseline reviewed login, the work queue, and the finalized debrief at 1280×720 and 390×844. It confirmed the permanent simulation warning, patient and encounter context, finalized release gate, labelled filters, URL filter state, live result announcement, contained narrow encounter orbit, no page-level horizontal overflow, and no captured console warnings/errors at either size.

The later `:8020` staged run exercised the actual browser interface from registration and check-in through nursing, medical assessment, result release and acknowledgement, pharmacy review and dispensing, encounter closure, RMIK quality review, independent ICD-10 and ICD-9-CM coding reviews, finalization, and the 35-event debrief. The desktop audit covered login, work queues, registration, arrived encounter, nursing intake/review, medical assessment/review, order/results, pharmacy, closure author/review, record-quality author/review, coding author/review, and debrief. Each audited page had one `h1`, no duplicate IDs, no page-level horizontal overflow, and permanent simulation labelling. The rebuilt login exposes exactly one named checkbox. Rebuilt clinical review, closure, record quality, and debrief pages were verified with exactly one `main`, no unnamed controls, and no captured browser warnings/errors on the corrected-page recheck.

An additional isolated `:8021` run used a browser-confirmed 390×844 viewport (device-pixel ratio 2) and the finalized 35-event synthetic fixture. It audited login, the work queue, registration, encounter overview, nursing intake, medical assessment, order/results, pharmacy, encounter closure, RMIK record quality, assisted coding, and finalized debrief across the applicable registrar, nursing, medical, pharmacy, and RMIK roles. Every audited route retained one `main`, one visible page `h1`, the permanent simulation label, no duplicate IDs, and no page-level horizontal overflow. Native radio controls in nursing remain 16 CSS pixels, but each is contained by a 316×54 CSS-pixel clickable label.

That run exposed and resolved three defects that were not visible in the earlier desktop pass: a finalized `DEBRIEF` task crashed the work queue because its icon/type mapping was absent; an unbroken finalization reason extended a timeline row to 636 CSS pixels inside a 356 CSS-pixel card; and the custom mobile-menu trigger did not receive focus after Escape. The exact rebuilt production assets were then rechecked at 390×844: the encounter timeline had no remaining internal overflow, the work queue rendered without a page error, the mobile menu returned visible focus to its trigger, breadcrumb links met the 44 CSS-pixel touch-height rule, and login exposed a single level-one heading plus the visual-order focus sequence `passkey → email → password → visibility → reset → remember → submit`. No new console warning or error was captured during the final production recheck.

Two further isolated SQLite fixtures at `:8022` and `:8023` exercised the active diagnosis- and procedure-source correction branches at a browser-confirmed 390×844 viewport using the exact supplied ICD-10 and ICD-9-CM workbooks. The diagnosis run covered the medical author queue, medical successor form, coder queue, blocked coding workspace, and amendment-pending encounter overview. The procedure run covered the closure-author queue and successor form, coder queue, and blocked procedure coding workspace. Each audited page retained one `main`, one visible `h1`, permanent simulation labelling, no duplicate IDs, no page-level horizontal overflow, and no enabled interactive control below the shared 44 CSS-pixel target. The exact task links and the save/submit actions accepted visible keyboard focus without submission.

The diagnosis form showed the named coder request and immutable source hash, required an attributed change reason, locked the original clinical occurrence time plus orders and prescriptions, and left the medical successor fields available to the exact author. The procedure form showed the named coder request and procedure/closure hashes, required a change reason, locked all non-procedure closure content and the original occurrence time, and left only performed-procedure documentation editable. In both branches the coder task was blocked. Its link opened the exact source: diagnosis with ICD-10 and procedure with ICD-9-CM. Candidate generation and source-system selection remained disabled, and the provenance gate showed `CODING · Diblokir`.

This run exposed and resolved two additional defects: the medical correction reason was enforced only by the domain service rather than exposed as required HTML, and a blocked procedure task opened the first diagnosis instead of the disputed procedure. Automated guardrails and feature tests now cover both corrections.

A final isolated SQLite fixture at `:8024` exercised the registrar's active check-in path at a browser-confirmed 390×844 viewport. The registrar work queue, pre-check-in registration workspace, arrived encounter overview, and post-check-in registration workspace each retained one `main`, one visible `h1`, permanent simulation labelling, no duplicate IDs, no page-level horizontal overflow, no enabled interactive control below 44 CSS pixels, and no captured browser warning or error. Before the action, patient search and the existing case's Check-in action remained enabled while the ten case-creation controls were effectively disabled by the fieldset described by `case-limit-message`. Check-in changed the server-backed encounter state from `PLANNED` to `ARRIVED`, created the waiting clinic-queue entry and `appointment_check_in` timeline event, and opened the shared encounter. On return, the appointment showed `Sudah check-in`, the Check-in action was absent, and the second-case fieldset remained disabled. Automated backend and frontend regressions cover the same state transitions and safeguard.

The shared teaching-evidence increment adds a separate `debrief.write` capability, transactional/idempotent note creation, append-only revisions with required reasons and hashes, participant read-only access, completed-session write lock, minimized audit metadata, and scenario-versioned non-scoring rubric references. Focused and full automated gates pass, including a fresh `001400` SQLite migration, exact official-workbook import, and completion of the synthetic reference encounter to `FINALIZED`.

The reporting increment adds a separate `report.view` capability plus two on-demand Blade projections: a source-derived outpatient summary and a curated debrief-evidence report. Automated feature coverage confirms finalized-state gating, exact case context, completed-session reads, private/no-store/no-index headers, human-reviewed ICD-10 and ICD-9-CM presentation, shared-note version history, non-scoring rubric status, raw-audit exclusion, and minimized `report.rendered` metadata. Both views carry permanent simulation and non-legal/non-FHIR language.

An isolated `:8027` fixture then imported the exact supplied ICD-10 and ICD-9-CM workbooks, completed `ENC-SIM-000001` to `FINALIZED`, and added one shared facilitator note before rendering both reports at 1280×720 and 390×844. Each report retained one `main`, one visible `h1`, no duplicate IDs, no page-level horizontal overflow, permanent simulation/non-legal language, 44 CSS-pixel screen actions, and no captured browser warning or error. At mobile width, all four outpatient tables remained contained by horizontally scrollable table wrappers while the page stayed 390 CSS pixels wide. The outpatient report showed the approved ICD-10 `R42` and ICD-9-CM `38.99` decisions; the debrief report showed the 35-event curated timeline, one shared note/version, and the non-scoring rubric reference. Database inspection found six `report.rendered` events containing only report type, section count, and source counts.

The report print action invoked `window.print()`, and browser inspection confirmed that the loaded stylesheet declares A4 portrait page settings, hides screen-only controls, preserves the simulation watermark, removes screen framing, and avoids internal breaks for major report blocks. The supported in-app browser does not expose print-media emulation or its native print preview, so native preview/PDF pagination and appearance remain pending and are not claimed.

An isolated `:8026` run then exercised the shared teaching-evidence interface at a browser-confirmed 390×844 viewport. The facilitator created an initial note, revised it from v1 through v3, created two different additional notes consecutively without reloading, expanded the append-only history, and observed each latest change reason. The participant saw the same latest note, history control, pending rubric reference, and explicit non-scoring boundary without any create/revise form or textarea. Database inspection found three logical notes with version ranges `1–3`, `1–1`, and `1–1`; three creation events and two revision events contained zero note-body fragments in audit metadata.

The first pass exposed four UI defects: successful create and revision forms retained their old request key and attestation state, the latest change reason was hidden, and the collapsed history target was only 20 CSS pixels high. The corrected production build cleared and refreshed both forms, closed the successful revision editor, displayed the latest reason, and increased the history target to 44 CSS pixels. Both final role views retained one `main`, one visible `h1`, permanent simulation labelling, no duplicate IDs, no page-level horizontal overflow, no enabled interactive control below 44 CSS pixels, and no captured browser warning or error.

An isolated `:8028` run then revisited the finalized reference journey at 1280×720 using the exact supplied terminology workbooks. Nineteen representative pages were opened across facilitator, registration, nursing, medical, pharmacy, and RMIK-coder accounts: each role's work queue; finalized registration, nursing, medical, result, pharmacy, closure, record-quality, coding, encounter-overview, and debrief views; and both report views. Every audited route retained one `main`, one visible `h1`, no duplicate IDs, no page-level horizontal overflow, no unnamed enabled focusable control, and no captured console warning or error.

This run does not close the keyboard gate. The selected browser-control surface retained a visible focus ring on the login email field but did not advance DOM focus when Tab or Shift+Tab was sent through any of its documented keyboard methods. Because the surface could not reproduce native sequential navigation, no full-page keyboard-order or visible-focus pass is claimed.

A later source-level accessibility review found that email autofocus contradicted the documented sign-in order and that server validation messages were not associated with their fields. A failing keyboard regression first reproduced the skipped passkey option. The corrected page starts from the first focusable control, follows the complete seven-control sequence, exposes `aria-invalid` plus stable error descriptions, and passes an axe scan in the validation state. Native Chrome automation could not be added because the ChatGPT Chrome Extension is not installed in the active profile; no browser setting was changed and the manual gate remains open.

A separate `:8029` fixture reached the active diagnosis-correction author state and rendered the exact medical learner work item. The browser safety layer then blocked navigation to the observed local correction URL before the form rendered. Its governing browser skill prohibits retrying through another browser surface or indirect workaround. The diagnosis/procedure correction-route console gate therefore remains open; this policy block is not treated as application success or failure.

A subsequent isolated `:8000` Checkpoint 2 rehearsal rebuilt three databases from the same seeded synthetic fixture and the exact verified ICD workbook hashes: one finalized journey, one active diagnosis correction, and one active procedure correction. The finalized command reached `FINALIZED` with two approved human coding assignments; both report routes rendered with one `main`, one `h1`, permanent simulation/non-legal language, no duplicate IDs, no page overflow, print CSS, and no captured console warning or error. The report pages exposed approved ICD-10 `R42`, ICD-9-CM `38.99`, the 35-event debrief timeline, and the non-scoring rubric boundary.

The same rehearsal discovered `UAT-20260716-001`: correction forms reduced second-precision source times to minute precision in HTML `datetime-local` controls and Inertia form state. The domain services correctly rejected a diagnosis successor closure whose locked performed-procedure times no longer matched the approved source. A procedure correction could also have silently changed the performed times while the author edited only its statement or body site. This was classified `MUST_FIX_BEFORE_UAT_RESUME`. Correction forms now use browser-compatible second-precision values (`YYYY-MM-DDTHH:mm:ss`) while ordinary authoring retains minute precision.

After rebuilding the frontend, two fresh disposable fixtures completed the full correction paths through the real browser interface. The diagnosis route created and approved medical v2, closure v2, replacement RMIK review v2, manual ICD-10 `R42`, and manual ICD-9-CM `38.99`; its correction reached `RESOLVED` and the encounter reached `FINALIZED`. Medical versions retained `2026-07-16 15:32:09`, while closure procedures retained `15:17:09–15:27:09` across both versions. The procedure route created and approved closure v2 plus replacement RMIK review v2, then approved the same two human assignments; its correction reached `RESOLVED` and the encounter reached `FINALIZED`. Only the procedure statement and body site changed, while performed times remained exactly `15:17:09–15:27:09`. No browser-visible application error occurred during either completed workflow. Focused regression tests guard browser-compatible second precision separately from ordinary minute-precision authoring.

The following broader checks remain pending and must not be reported as passed:

- full-page keyboard traversal and visible-focus review beyond sign-in, mobile-menu focus return, and the verified correction and debrief task/save/submit actions; and
- one uninterrupted browser-console capture across both complete correction routes (the functional legs now pass, but a retained console capture is not claimed); and
- native print-preview/PDF pagination and appearance review of both finalized report views.

Automated axe component tests reduce but do not remove this manual evidence requirement. Browser/manual evidence is required before combined end-to-end UAT.

## Scope statement

This record validates the implemented synthetic outpatient reference journey from active registration/check-in through nursing, medicine, results, pharmacy, closure, RMIK, human-reviewed coding, finalization, read-only debrief projection, shared versioned debrief-note API/read model and narrow browser behavior, non-scoring rubric references, automated finalized-report contracts, responsive finalized-report browser behavior, finalized representative-role console behavior, and complete functional diagnosis/procedure coding-source correction branches. Separate records cover computer-assisted coding and the local full-journey OPS-01 backup/restore rehearsal. Final report selection, signed/persisted documents, managed disclosures, rubric scoring, native print-preview/PDF pagination, deployment/OPS-02, full native keyboard traversal, retained uninterrupted correction-route console capture, and stakeholder acceptance remain outside this record.
